{
  "name": "spec",
  "version": "0.1.0",
  "description": "Driving Instructor Booking API",
  "scripts": {
    "start": "prism mock -h 127.0.0.1 -p 8000 ./spec.yaml",
    "start:dev": "concurrently \"node server.js\" \"prism mock -h 127.0.0.1 -p 8001 ./spec.yaml\""
  },
  "dependencies": {
    "@stoplight/prism-cli": "^4.10.1",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "concurrently": "^8.2.0"
  }
}